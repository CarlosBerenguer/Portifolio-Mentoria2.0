{
  "name": "cypress",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --e2e --browser electron",
    "cy:open": "cypress open --e2e --browser electron",
    "test:cloud": "cypress run --record"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/e2e/support/steps/**/*.{js,ts}",
      "QA/Cypress/cypress/e2e/support/steps/**/*.{js,ts}"
    ]
  },
  
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.5",
    "cypress": "^15.0.0"
  }
}
